# browser_history_member

## How locally test the code
1. create and activate an environment with `requirements.txt` and `python` (highly suggested `3.12` version!)
2. `python -m pytest test/` to run all tests
